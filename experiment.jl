using Suppressor

function experiment_step(f, beta, dgp, estimate)

end

function naive_step(f, beta, dgp)

end

function frankel_dgp(f, beta)

end

function nonconvex_dgp(f, beta)

end

function gradient_experimental()

end

#use Flux to optimize neural network on batch of data
function true_nonconvex(y, x, gradients)

end

function true_frankel(a, b)

end

function ols(y, x)

end
